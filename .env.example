# PayCrivo frontend — point this at your self-hosted backend (Express + Prisma + Postgres in /server).
# Leave EMPTY in the Lovable preview to use graceful mock/fallback mode.
# Example (local backend): VITE_API_BASE_URL=http://localhost:4100
VITE_API_BASE_URL=
